/* function solve(num){
    for (let i = 1; i <= 10; i++){
        let result = i * num;
        console.log(`${num} X ${i} = ${result}`);
    }
}
solve(6);//100/100 */

function solve(num){
    for (let i = 1; i <= 10; i++){
        console.log(`${num} X ${i} = ${i * num}`);
    }
}
solve(6);//100/100